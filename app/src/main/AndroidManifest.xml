<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.link.sangsang">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Sangsang">
        <activity
            android:name=".activity.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activity.AdActivity" />
        <activity android:name=".activity.BenefitActivity" />
        <activity android:name=".activity.BenefitDetailActivity" />
        <activity android:name=".activity.CommunityActivity" />
        <activity android:name=".activity.PointActivity" />
        <activity android:name=".activity.PointDetailActivity" />
        <activity android:name=".activity.ShopActivity" />
        <activity android:name=".activity.SurveyActivity" />
    </application>

</manifest>